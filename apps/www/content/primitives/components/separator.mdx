---
title: Separator
description: A visual divider that separates content with support for vertical and horizontal orientations.
radix:
  link: https://www.radix-ui.com/docs/primitives/components/separator
  api: https://www.radix-ui.com/docs/primitives/components/separator#api-reference
---

## Preview

<Preview>
  <Flex style={{ gap: "24px", flexDirection: "column", width: "100%" }}>
    <Separator size="small" />
    <Separator size="half" />
    <Separator size="full" />
  </Flex>
</Preview>

## Usage

The Separator component creates a separation between content sections.

## Installation

Install the component from your command line.

<LiveProvider>
  <LiveEditor code={`npm install @raystack/apsara`} border language="shell" />
</LiveProvider>

<LiveProvider>
  <LiveEditor code={`import { Separator } from '@raystack/apsara/v1'

// Basic usage

<Separator />

// With custom size

<Separator size="half" />

// Vertical orientation

<Separator orientation="vertical" size="small" />`} border/>
</LiveProvider>

## Props

The Separator component accepts the following props:

- `size`: Defines the length of the separator (`"small"` | `"half"` | `"full"`, default: "full")
- `color`: Sets the color of the separator (`"primary"` | `"secondary"` | `"tertiary"`, default: "primary")
- `orientation`: Sets the direction of the separator (`"horizontal"` | `"vertical"`, default: "horizontal")
- `className`: Additional CSS class names

## Colors

The Separator component comes in three colors to match different design contexts.

<Playground
  scope={{ Separator, Flex }}
  tabs={[
    {
      name: "Primary",
      code: `<Flex style={{ padding: '16px' }}>
  <Separator color="primary" />
</Flex>`,
    },
    {
      name: "Secondary",
      code: `<Flex style={{ padding: '16px' }}>
  <Separator color="primary" />
</Flex>`,
    },
    {
      name: "Tertiary",
      code: `<Flex style={{ padding: '16px' }}>
  <Separator color="primary" />
</Flex>`,
    },
  ]}
/>

## Sizes

The Separator component comes in three sizes to accommodate different layout needs.

<Playground
  scope={{ Separator, Flex }}
  tabs={[
    {
      name: "Small",
      code: `<Flex style={{ padding: '16px', width: '100%' }}>
  <Separator size="small" />
</Flex>`,
    },
    {
      name: "Half",
      code: `<Flex style={{ padding: '16px', width: '100%' }}>
  <Separator size="half" />
</Flex>`,
    },
    {
      name: "Full",
      code: `<Flex style={{ padding: '16px', width: '100%' }}>
  <Separator size="full" />
</Flex>`,
    },
  ]}
/>

## Orientation

Separator can be rendered in both horizontal and vertical orientations.

<Playground
  scope={{ Separator, Flex }}
  tabs={[
    {
      name: "Horizontal",
      code: `<Separator orientation="horizontal" size="full" />`,
    },
    {
      name: "Vertical",
      code: `
<Flex style={{ height: '50px', alignItems: 'center', gap: '16px' }}>
  Content
  <Separator orientation="vertical" size="small" />
  Content
</Flex>`,
    },
  ]}
/>

## Examples

### Basic Separator

<Playground
  scope={{ Separator }}
  code={`
<Flex style={{ gap: '16px', flexDirection: 'column' }}>
  <div>Above content</div>
  <Separator />
  <div>Below content</div>
</Flex>
  `}
/>

### Content Groups

<Playground
  scope={{ Separator, Flex }}
  code={`
<Flex style={{ gap: '16px', alignItems: 'center' }}>
  <span>Group 1</span>
  <Separator orientation="vertical" size="small" />
  <span>Group 2</span>
  <Separator orientation="vertical" size="small" />
  <span>Group 3</span>
</Flex>
  `}
/>

### Custom Spacing

<Playground
  scope={{ Separator, Flex }}
  code={`
<Flex style={{ gap: '24px', flexDirection: 'column' }}>
  <div>Section 1</div>
  <Separator size="half" />
  <div>Section 2</div>
  <Separator size="half" />
  <div>Section 3</div>
</Flex>
  `}
/>

### Custom Styling

<Playground
  scope={{ Separator, Flex }}
  code={`
<Flex style={{ gap: '24px', flexDirection: 'column' }}>
  <div>Section 1</div>
  <Separator size="half" color="secondary" />
  <div>Section 2</div>
  <Separator size="half" color="tertiary" />
  <div>Section 3</div>
</Flex>
  `}
/>

## Accessibility

The Separator component follows accessibility best practices:

- Uses `role="separator"` by default
- Is marked as `decorative` for visual separation
- Maintains proper color contrast
- Supports proper semantic structure in both orientations

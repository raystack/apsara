---
title: Select
description: Displays a list of options for the user to pick from—triggered by a button.
radix:
  link: https://www.radix-ui.com/docs/primitives/components/select
  api: https://www.radix-ui.com/docs/primitives/components/select#api-reference
---

## Preview

<Preview>
  <Select>
    <Select.Trigger width={200} size="small">
      <Select.Value placeholder="Select a fruit" />
    </Select.Trigger>
    <Select.Content>
      <Select.Group>
        <Select.Item value="apple">Apple</Select.Item>
        <Select.Item value="banana">Banana</Select.Item>
        <Select.Item value="blueberry">Blueberry</Select.Item>
        <Select.Item value="grapes">Grapes</Select.Item>
        <Select.Item value="pineapple">Pineapple</Select.Item>
      </Select.Group>
    </Select.Content>
  </Select>
</Preview>

## Installation

<LiveProvider>
  <LiveEditor code={`npm install @raystack/apsara`} border language="shell" />
</LiveProvider>

## Usage

<LiveProvider>
  <LiveEditor code={`import { Select } from '@raystack/apsara/v1'`} border />
</LiveProvider>

## Props

### Select.Trigger

- `size`: Defines the size of the trigger (`"small"` | `"medium"`, default: `"medium"`)
- `variant`: Visual style variant (`"default"` | `"filter"`, default: `"default"`)
- `iconProps`: Props for the chevron icon
- `aria-label`: Accessible label
- `aria-describedby`: ID of element describing the select
- `aria-required`: Whether the select is required
- `aria-invalid`: Whether the select has an invalid value

### Select.Content

- `position`: Position of the content (`"popper"` by default)
- `className`: Additional CSS classes

### Select.Item

- `value`: The value of the item
- `textProps`: Props passed to the Text component
- `className`: Additional CSS classes

## Examples

### Basic Select

<Playground
  scope={{ Select }}
  code={`
<Select>
  <Select.Trigger aria-label="Fruit selection">
    <Select.Value placeholder="Select a fruit" />
  </Select.Trigger>
  <Select.Content>
    <Select.Item value="apple">Apple</Select.Item>
    <Select.Item value="banana">Banana</Select.Item>
  </Select.Content>
</Select>
`}
/>

### Small Size

<Playground
  scope={{ Select }}
  code={`
<Select>
  <Select.Trigger size="small">
    <Select.Value placeholder="Small select" />
  </Select.Trigger>
  <Select.Content>
    <Select.Item value="1">Option 1</Select.Item>
    <Select.Item value="2">Option 2</Select.Item>
  </Select.Content>
</Select>
`}
/>

### Filter Variant

<Playground
  scope={{ Select }}
  code={`
<Select>
  <Select.Trigger variant="filter">
    <Select.Value placeholder="Filter..." />
  </Select.Trigger>
  <Select.Content>
    <Select.Item value="all">All</Select.Item>
    <Select.Item value="active">Active</Select.Item>
    <Select.Item value="inactive">Inactive</Select.Item>
  </Select.Content>
</Select>
`}
/>

### With Separator

<Playground
  scope={{ Select }}
  code={`
<Select>
  <Select.Trigger>
    <Select.Value placeholder="Select..." />
  </Select.Trigger>
  <Select.Content>
    <Select.Group>
      <Select.Item value="1">Option 1</Select.Item>
      <Select.Item value="2">Option 2</Select.Item>
    </Select.Group>
    <Select.Separator />
    <Select.Group>
      <Select.Item value="3">Option 3</Select.Item>
      <Select.Item value="4">Option 4</Select.Item>
    </Select.Group>
  </Select.Content>
</Select>
`}
/>

## Accessibility

The Select component follows WAI-ARIA guidelines:

- Trigger has role `combobox`
- Content has role `listbox`
- Items have role `option`
- ARIA labels and descriptions

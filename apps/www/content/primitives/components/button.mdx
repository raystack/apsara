---
name: button
title: Button
description: Displays a button or a component that looks like a button.
---
import { Button, Flex } from '@odpf/apsara'

## Basic
The basic `Button`
<Playground scope={{ Button }} code={`<Button>Basic button</Button>`} />

## Primary
The primary `Button`
<Playground scope={{ Button }} code={`<Button variant="primary">Basic button</Button>`} />

## Scondry
The secondary `Button`
<Playground scope={{ Button }} code={`<Button variant="secondary">Basic button</Button>`} />

## Outline
The Outline `Button`
<Playground scope={{ Button }} code={`<Button variant="secondary" outline>Basic button</Button>`} />

## Disabled

Do not respond to any action.
<Playground scope={{ Button }} code={`<Button variant="secondary" disabled>Basic button</Button>`} />

## Scale
Buttons of different sizes.
<Playground scope={{ Button, Flex }} code={`
  <Flex align="center" css={{gap: "8px"}}>
    <Button variant="primary" size="small">Basic button</Button>
    <Button variant="primary">Basic button</Button>
    <Button variant="primary" size="large">Basic button</Button>
    <Button variant="primary" size="circle">B</Button>
  </Flex>
`} />


## Installation

Install the component from your command line.

```bash
npm install @odpf/apsara
```


## Anatomy
Import all parts and piece them together.

```tsx outline=true
import { Button } from '@odpf/apsara'

<Button>Basic button</Button>

<Button variant="secondary">Basic button</Button>
<Button variant="secondary" size="large">Basic button</Button>
<Button variant="secondary" outline>Basic button</Button>
<Button variant="secondary" filled={false}>Basic button</Button>
```

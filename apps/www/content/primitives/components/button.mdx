---
name: button
title: Button
description: Displays a button or a component that looks like a button.
---

import {  Button } from "@raystack/apsara";



<Preview>
    <Button variant="primary">Primary button</Button>
</Preview>

<Description>
    Buttons are fundamental components in any user interface, serving as interactive elements for triggering actions or
    navigating through an application. Building reusable button primitives using Radix UI and Stitches CSS-in-JS can
    significantly enhance the efficiency and maintainability of your UI development process.
</Description>

## Installation
Install the component from your command line.

<LiveProvider>
    <LiveEditor code={`npm install @raystack/apsara`} border/>
</LiveProvider>


## Usages

Import all parts and piece them together.

<LiveProvider>
    <LiveEditor code={`
import { Button } from '@raystack/apsara'

<Button variant="primary">primary button</Button>`} border />
</LiveProvider>


## Basic

The basic `Button`

<Playground scope={{ Button }} code={`<Button>button</Button>`} />

## Variant

Diffrent button variants

<Playground
    scope={{ Button }}
    tabs={[
        {
            name: "Primary",
            code: `<Button variant="primary">Primary button</Button>`,
        },
        {
            name: "Outline",
            code: `<Button variant="outline">Outline button</Button>`,
        },
        {
            name: "Secondary",
            code: `<Button variant="secondary">Secondary button</Button>`,
        },
        {
            name: "Ghost",
            code: `<Button variant="ghost">Ghost button</Button>`,
        },
        {
            name: "Danger",
            code: `<Button variant="danger">Danger button</Button>`,
        },
    ]}
/>

## Scale

Buttons of different sizes.

<Playground
    scope={{ Button }}
    tabs={[
        
        {
            name: "Small",
            code: `<Button variant="primary" size="small">primary button</Button>`,
        },
        {
            name: "Medium",
            code: `<Button variant="primary" size="medium">primary button</Button>`,
        },
        {
            name: "Default",
            code: `<Button variant="primary">primary button</Button>`,
        },
    ]}
/>


## Disabled

Do not respond to any action.

<Playground scope={{ Button }} code={`
<div style={{display:'flex', gap: '8px'}}>
    <Button variant="primary" disabled>secondary button</Button>
    <Button variant="outline" disabled>secondary button</Button>
    <Button variant="secondary" disabled>secondary button</Button>
    <Button variant="ghost" disabled>secondary button</Button>
    <Button variant="danger" disabled>secondary button</Button>
</div>
`} />


## API Reference

<Attributes type="button" />

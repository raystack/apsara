---
title: Callout
description: A visual message component to attract user's attention.
---

## Preview

<Preview>
  <Flex
    style={{
      flexDirection: "column",
      alignItems: "start",
      gap: "8px",
    }}
  >
    <Callout type="success">
      A short message to attract user's attention
    </Callout>
    <Callout type="alert">A short message to attract user's attention</Callout>
    <Callout type="gradient">
      A short message to attract user's attention
    </Callout>
    <Callout type="accent">A short message to attract user's attention</Callout>
    <Callout type="attention">
      A short message to attract user's attention
    </Callout>
    <Callout type="normal">A short message to attract user's attention</Callout>
  </Flex>
</Preview>

## Installation

Install the component from your command line.

<LiveProvider>
  <LiveEditor code={`npm install @raystack/apsara`} border />
</LiveProvider>

Import and usage.

<LiveProvider>
    <LiveEditor code={`
import { Callout } from '@raystack/apsara/v1'

<Callout type="success">A short message to attract user's attention</Callout>
`} border />
</LiveProvider>

## Props

The `Callout` component accepts the following props:

- `type`: Visual style variant (`"grey"` | `"success"` | `"alert"` | `"gradient"` | `"accent"` | `"attention"` | `"normal"`, default: "grey")
- `outline`: Whether to show an outline border (`boolean`, default: false)
- `highContrast`: Whether to use high contrast colors (`boolean`, default: false)
- `action`: Optional action element to display on the right
- `dismissible`: Whether to show a dismiss button (`boolean`, default: false)
- `onDismiss`: Callback function when dismiss button is clicked
- `width`: Custom width for the callout
- `className`: Additional CSS class names
- `style`: Additional CSS styles
- All other HTMLDivElement attributes from React

## Type

The Callout component offers different type options. You can customize the visual style using the `type` prop:

<Playground
  scope={{ Callout }}
  tabs={[
    {
      name: "Grey",
      code: `<Callout type="grey">A short message to attract user's attention</Callout>`,
    },
    {
      name: "Success",
      code: `<Callout type="success">A short message to attract user's attention</Callout>`,
    },
    {
      name: "Alert",
      code: `<Callout type="alert">A short message to attract user's attention</Callout>`,
    },
    {
      name: "Gradient",
      code: `<Callout type="gradient">A short message to attract user's attention</Callout>`,
    },
    {
      name: "Accent",
      code: `<Callout type="accent">A short message to attract user's attention</Callout>`,
    },
    {
      name: "Attention",
      code: `<Callout type="attention">A short message to attract user's attention</Callout>`,
    },
    {
      name: "Normal",
      code: `<Callout type="normal">A short message to attract user's attention</Callout>`,
    },
  ]}
/>

## Outline

The Callout component can be rendered with or without an outline border:

<Playground
  scope={{ Callout }}
  tabs={[
    {
      name: "Without Outline",
      code: `<Callout type="success">A short message to attract user's attention</Callout>`,
    },
    {
      name: "With Outline",
      code: `<Callout type="success" outline>A short message to attract user's attention</Callout>`,
    },
  ]}
/>

## High Contrast

The Callout component supports high contrast mode for better visibility:

<Playground
  scope={{ Callout }}
  tabs={[
    {
      name: "Normal Contrast",
      code: `<Callout type="success">A short message to attract user's attention</Callout>`,
    },
    {
      name: "High Contrast",
      code: `<Callout type="success" highContrast>A short message to attract user's attention</Callout>`,
    },
  ]}
/>

## With Action

The Callout component can include an action button:

<Playground
  scope={{ Callout }}
  tabs={[
    {
      name: "With Action",
      code: `<Callout 
  type="success" 
  action={<button>Action</button>}
>
  A short message to attract user's attention
</Callout>`,
    },
  ]}
/>

## Dismissible

The Callout component can be made dismissible:

<Playground
  scope={{ Callout }}
  tabs={[
    {
      name: "Dismissible",
      code: `<Callout 
  type="success" 
  dismissible 
  onDismiss={() => console.log('dismissed')}
>
  A short message to attract user's attention
</Callout>`,
    },
  ]}
/>

## Custom Width

The Callout component supports custom width:

<Playground
  scope={{ Callout }}
  tabs={[
    {
      name: "Custom Width",
      code: `<Callout type="success" width={500}>A short message to attract user's attention</Callout>`,
    },
  ]}
/>

## Accessibility

The Callout component includes appropriate ARIA attributes for accessibility:

- Uses semantic HTML elements for proper structure
- Dismiss button includes `aria-label` for screen readers
- Interactive elements are keyboard accessible

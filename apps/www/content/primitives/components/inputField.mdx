---
title: Input Field
description: A text input component with support for labels, helper text, and various states.
---

<Preview>
  <InputField label="Default" placeholder="Enter text" width="300px" />
</Preview>

## Usage

Input Field component is used for collecting user input in forms and interfaces, with support for various states and decorative elements.

## Installation

Install the component from your command line.

<LiveProvider>
  <LiveEditor code={`npm install @raystack/apsara`} border />
</LiveProvider>

Import all parts and piece them together.

<LiveProvider>
  <LiveEditor code={`
import { InputField } from '@raystack/apsara/v1'

<InputField label="Label" placeholder="Enter text" />`} border />
</LiveProvider>

## Props

The `InputField` component accepts the following props:

- `label`: Text label above the input (string)
- `helperText`: Helper text below the input (string)
- `error`: Error message to display below the input (string)
- `disabled`: Whether the input is disabled (boolean)
- `leadingIcon`: Icon element to display at the start of input (ReactNode)
- `trailingIcon`: Icon element to display at the end of input (ReactNode)
- `optional`: Shows "(Optional)" text next to label (boolean)
- `prefix`: Text or symbol to show before input value (string)
- `suffix`: Text or symbol to show after input value (string)
- `width`: Custom width for the input field (string | number)
- `className`: Additional CSS class names
- All other InputHTMLAttributes from React

## Variants

### Basic Input

A basic input field with a label and placeholder.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="Default"
  placeholder="Enter text"
/>`}
/>

### With Helper Text

Input field with additional helper text below.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="With label"
  placeholder="Enter text"
  helperText="This is a helper text"
/>`}
/>

### With Error

Input field displaying an error state with error message.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="With Error"
  placeholder="Enter text"
  error="This field is required"
/>`}
/>


### With Prefix/Suffix

Input field with prefix and suffix text.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="With Prefix/Suffix"
  placeholder="0.00"
  prefix="$"
  suffix="USD"
/>`}
/>


### With Icons

Input field with leading and trailing icons.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="With Icons"
  placeholder="Enter text"
  leadingIcon={<>O</>}
  trailingIcon={<>O</>}
/>`}
/>

### Optional Field

Input field marked as optional.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="Optional Field"
  placeholder="Enter text"
  optional
/>`}
/>

### Disabled State

Input field in disabled state.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="Disabled"
  placeholder="Enter text"
  disabled
/>`}
/>

### Custom Width

Input field with custom width.

<Playground
  scope={{ InputField }}
  code={`
<InputField 
  label="Custom Width"
  placeholder="Enter text"
  width="300px"
/>`}
/>
---
title: TextArea
description: A multi-line text input field with support for labels and helper text.
source: packages/raystack/components/textarea
---

import {
  playground,
  basicDemo,
  controlledDemo,
  widthDemo,
  errorDemo,
} from "./demo.ts";

<Demo data={playground} />

## Anatomy

Import and assemble the component:

```tsx
import { TextArea } from "@raystack/apsara";

<TextArea />
```

## API Reference

<auto-type-table path="./props.ts" name="TextAreaProps" />

## Examples

### Basic Usage

The most basic usage of TextArea with just a label and placeholder.

<Demo data={basicDemo} />

### Error State

TextArea in error state with custom styling.

<Demo data={errorDemo} />

### Controlled Usage

Example of TextArea in controlled mode.

<Demo data={controlledDemo} />

### Custom Width

TextArea with custom width specification.

<Demo data={widthDemo} />

## Accessibility

- Associates label with textarea via `htmlFor` and `id` attributes
- Error messages are linked with `aria-describedby`
- Required state is communicated via `aria-required`
- Invalid state is communicated via `aria-invalid`

---
title: TextArea
description: A multi-line text input field.
source: packages/raystack/components/text-area
---

import {
  playground,
  basicDemo,
  controlledDemo,
  widthDemo,
  withFieldDemo,
} from "./demo.ts";

<Demo data={playground} />

## Anatomy

Import and assemble the component:

```tsx
import { TextArea } from "@raystack/apsara";

<TextArea placeholder="Enter text" />
```

For labels, description, and error messages, wrap with [Field](/docs/components/field):

```tsx
import { Field, TextArea } from "@raystack/apsara";

<Field label="Bio" description="Tell us about yourself" error={errors.bio?.message}>
  <TextArea placeholder="Write something..." />
</Field>
```

## API Reference

Renders a multi-line text input. For label, description, and error support, use with [Field](/docs/components/field).

<auto-type-table path="./props.ts" name="TextAreaProps" />

## Examples

### Basic Usage

A basic TextArea with a placeholder.

<Demo data={basicDemo} />

### With Field

Use Field to add label, description, and error handling.

<Demo data={withFieldDemo} />

### Controlled Usage

Example of TextArea in controlled mode.

<Demo data={controlledDemo} />

### Custom Width

TextArea with custom width specification.

<Demo data={widthDemo} />

## Accessibility

- Use with [Field](/docs/components/field) for automatic label association and error linking
- Required state is communicated via `aria-required`
- Invalid state is communicated via `aria-invalid`

---
title: Sidepanel
description: A flexible side panel component to place content on either side of the page.
---

import { basicDemo, positionDemo, preview } from "./demo.ts";

<Demo data={preview} />

## Usage

```tsx
import { SidePanel } from "@raystack/apsara";
```

## SidePanel Props

Customize the SidePanel appearance with these configuration options.

The `SidePanel` component is composed of several parts, each with their own props

<auto-type-table path="./props.ts" name="SidePanelProps" />

### SidePanel.Header

<auto-type-table path="./props.ts" name="SidePanelHeaderProps" />

## Examples

### Basic Usage

<Demo data={basicDemo} />

### Position

The SidePanel can be positioned on either the left or right side of the screen.

<Demo data={positionDemo} />

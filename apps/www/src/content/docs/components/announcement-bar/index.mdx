---
title: Announcement Bar
description: AnnouncementBar component to display message
source: packages/raystack/components/announcement-bar
---

import {
  playground,
  variantsDemo,
  sizesDemo,
  colorsDemo,
  disabledDemo,
  loadingDemo,
  iconsDemo,
} from "./demo.ts";

<Demo data={playground} />

## Anatomy

Import and assemble the component:

```tsx
import { AnnouncementBar } from '@raystack/apsara'

<AnnouncementBar />
```

## API Reference

Renders a dismissible banner for announcements and alerts.

<auto-type-table path="./props.ts" name="AnnouncementBarProps" />

## Accessibility

- The action (when `actionLabel` or `actionIcon` is provided) is rendered
  as a real `<button>`, so it's focusable, reachable by Tab, and activated
  by Enter/Space — not as a `<Text>` with an `onClick` handler.
- The action button exposes a focus ring via `:focus-visible`.
- Decorative `leadingIcon` and `actionIcon` are marked `aria-hidden="true"`
  so they don't add noise to screen readers.

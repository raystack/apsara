---
title: List
description: A component for displaying information in a key-value pair list format.
source: packages/raystack/components/list
---

import { basicDemo, preview,
  alignDemo,
} from "./demo.ts";

<Demo data={preview} />

## Anatomy

Import and assemble the component:

```tsx
import { List } from "@raystack/apsara";

<List>
  <List.Header />
  <List.Item>
    <List.Label />
    <List.Value />
  </List.Item>
</List>
```

## Usage

A list is rows of label and value. The only real choice is how those two columns line up.

### Basic usage

A `Header` labels the group, then each `Item` pairs a `Label` with its `Value`. Give the labels a shared `min-width` so the values line up in a column.

<Demo data={basicDemo} />

### Aligning rows

`align` on an `Item` controls how the label sits against its value. `center` suits one-line values; switch to `start` when the value wraps, so the label stays level with the first line rather than floating in the middle.

<Demo data={alignDemo} />

## API Reference

Each item holds an aligned label and value, with an optional header above them.

### Root

Groups all parts of the list.

<auto-type-table path="./props.ts" name="ListRootProps" />

### Header

Renders a header section above the list items.

<auto-type-table path="./props.ts" name="ListHeaderProps" />

### Item

Renders an individual list row.

<auto-type-table path="./props.ts" name="ListItemProps" />

### Label

Renders the key text within a list item.

<auto-type-table path="./props.ts" name="ListLabelProps" />

### Value

Renders the value text within a list item.

<auto-type-table path="./props.ts" name="ListValueProps" />

### Slots

Every rendered part carries a stable `data-slot` attribute for [styling and testing](/docs/styling#with-data-slot):

| Slot | Element |
|------|---------|
| `list` | The root `<ul>` element |
| `list-header` | The header row (`<div>` with heading role) |
| `list-header-text` | The `<span>` wrapping the header text |
| `list-item` | An individual list row (`<li>`) |
| `list-label` | The `<span>` holding the key text of an item |
| `list-value` | The `<span>` holding the value text of an item |

## Accessibility

The List component implements proper ARIA attributes for accessibility:

- List renders a `<ul>` with explicit `role="list"`. The role is kept
  even though the element is native, because Safari/VoiceOver drop the
  implicit list role when `list-style: none` is applied.
- List relies on the native `<li>` semantics for items, with no redundant
  `role="listitem"` is set.
- No generic default `aria-label` is added. Pass `aria-label` when the
  list needs an accessible name distinct from surrounding context.
- List.Header has `role="heading"` and forwards `aria-level` (1–6, defaults
  to 3) to the underlying heading so the rank matches the document outline.

import { Meta, Preview, Story, ArgsTable, IconGallery, IconItem } from "@storybook/addon-docs/blocks";
import Icon from "./Icon";
import * as Icons from "@goto-company/icons";
export const iconsList = Object.keys(Icons);

<Meta
    title="General/Icons"
    argTypes={{
        name: {
            name: "Name",
            description: "Icon name",
            control: { type: "select", options: iconsList },
            table: {
                type: { summary: "string" },
                defaultValue: { summary: "stop" },
            },
        },
        size: {
            description: "Icon size",
            control: { type: "range", min: 24, max: 320 },
            table: {
                type: { summary: "number" },
                defaultValue: { summary: 48 },
            },
        },
        color: {
            control: { type: "color" },
            description: "color",
            table: {
                type: { summary: "string" },
                defaultValue: { summary: "black" },
            },
        },
        onClick: { action: "icon clicked" },
    }}
/>

# Icons

<IconGallery>
    {iconsList.map((name, index) => (
        <IconItem key={name} name={name}>
            <Icon name={name} size={36} />
        </IconItem>
    ))}
</IconGallery>

## Playground

<Preview>
    <Story
        name="Playground"
        args={{
            name: "stop",
            size: 48,
            color: "black",
            disabled: false,
        }}
    >
        {(args) => <Icon {...args} styleOverride={{ color: args.color }} />}
    </Story>
</Preview>

<ArgsTable story="Playground" />

## Props

<ArgsTable of={Icon} />

import { Meta, Preview, Canvas, Story, ArgsTable, IconGallery, IconItem } from "@storybook/addon-docs/blocks";
import Switch from "./Switch";


<Meta
    title="General/Switch"
    argTypes={{
        checked: {
            control: { type: "boolean" },
            description: "Determine whether the Switch is checked",
            table: {
                type: { summary: "boolean" },
                defaultValue: { summary: "false" },
            },
        },
        defaultChecked: {
            control: { type: "boolean" },
            description: "Whether to set the initial state",
            table: {
                type: { summary: "boolean" },
                defaultValue: { summary: "false" },
            },
        },
        color:{
            control:{ type: "text"},
            description: "Color of switch when checked.",
            table: {
                type: { summary: "string" },
                defaultValue: { summary: "black" },
            },
        },
        onChange: {
             action: "switch state changed",
             description: "whenever switch state change's it will be called",
             table: {
                type: { summary: "(state: boolean)=>void" },
            },
        },
        name: {
            description:"Used when switch is used in Form",
            table:{
                type: { summary: "string" },
            }
        },
        value: {
            description:"value that will be submitted in a form",
            table:{
                type: { summary: "string" },
            }
        }
    }}
/>

<Preview>
    <Story
        name="Playground"
        args={{
            checked: true,
            color: "red"
        }}
    >
        {(args) => <Switch {...args} />}
    </Story>
</Preview>

# Props

<ArgsTable story="Playground" />

import { Meta, Preview, Canvas, Story, ArgsTable, IconGallery, IconItem } from "@storybook/addon-docs/blocks";
import Text, { styleMap, defaultSize } from "./Text";

export const sizes = Object.keys(styleMap);

<Meta
    title="General/Text"
    argTypes={{
        size: {
            description: "size",
            control: { type: "select", options: sizes },
            table: {
                type: { summary: "number" },
                defaultValue: { summary: defaultSize },
            },
        },
        color: {
            description: "color",
            table: {
                type: { summary: "string" },
                defaultValue: { summary: "black" },
            },
            control: { type: "color" },
        },
    }}
/>

# Texts

<Preview>
    <Story
        name="Playground"
        args={{
            size: 14,
            color: "black",
        }}
    >
        {(args) => <Text {...args} style={{ color: args?.color }}>{`${args?.size} Apsara`}</Text>}
    </Story>
</Preview>

<ArgsTable story="Playground" />
